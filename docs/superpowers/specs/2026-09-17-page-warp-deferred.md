# 页面矫正（页面弯曲导致同一视觉行高度不一）——挂起记录

日期：2026-09-17
状态：**挂起，未实现**。解锁条件见文末。
用户决定日期：2026-09-17（「记录，和文字分流一起再议」）。
姊妹文档：[`2026-09-17-text-flow-filter-deferred.md`](2026-09-17-text-flow-filter-deferred.md)
——两者是当初搁置的两个特性，**再议的时机相同**。

## 它现在挡着两个真实缺陷

真机那张英语卷子（`IMG_0004.HEIC`）上，`pageBody` 剔掉右缘手写旁注之后
（见 `a0aaa00`），整页塌成 1 个块、58 个视觉行，正文不再与旁注交错。
**剩下两个缺陷都来自同一件事：同一视觉行在页面不同横坐标上高度不同。**

### #2 页眉那张 2×6 表格整行倒着读

实际输出（第 4–15 段）：

```
正确率 / 实际用时 / 建议用时 / 词数 / 题材 / 文体 / 14 / mins / 8 mins / 348 / 新旧文化冲突 / 夹叙夹议
```

正确读序（行主序）：

```
文体 题材 词数 建议用时 实际用时 正确率 / 夹叙夹议 新旧文化冲突 348 8 mins /4
```

表格本身是**带边框的两行六列**（我裁图看过）。六个格子在同一行，但倾斜让它们
的 `top` 单调递减，`visualLines` 按 `anchorTop` 一排就成了从右到左。

### #3 第 2 题的 D 排到了第 3 题标题之后

实际输出：

```
33. What does Shirley Fitzgerald think of Sydney?   ← 第 3 题的题干
34. D. He is attached to the old ferries.           ← 第 2 题的 D 选项
```

选项区是 2×2 网格（`A|B` 一行、`C|D` 一行，裁图确认）。`D` 与第 3 题题干的
`top` **完全相同（都是 0.723）**，`minX` 分别是 0.503 和 0.111。`visualLines`
末尾那句 `lines.sorted { anchorTop($0) < anchorTop($1) }` 在平局时**没有回退
判据**（`sorted(by:)` 不保证稳定），谁在前是任意的。

## 实测：这不是整体旋转，是局部弯曲

| 位置 | 量法 | 斜率 dTop/dX |
|---|---|---|
| 页眉表格（y≈0.10） | 文体中心 x 0.2225→top 0.110；正确率中心 x 0.777→top 0.100 | **−0.018**（向右上斜） |
| 底部选项区（y≈0.70） | A 中心 x 0.219→top 0.687；B 中心 x 0.636→top 0.704 | **≈ +0.04**（向右下斜） |

底部那一行有一个前提：**A 与 B 是同一视觉行**。这一条不是从几何推出来的，
是**裁图看出来的**——右栏整体比左栏低约 0.017，而该处行距只有 0.020。
换成 C|D 一对算得 +0.0385，两对一致。

同一页上下两处斜率**符号相反**，所以任何**单一旋转角**都矫正不了它。
（顺带：这也说明它不是「相机没端平」那么简单。）

## 为什么「按纵向重叠分行」这条判据在这页上失效

这是修 #2 最自然想到的判据，**实测分不开**：

| 该分成同一行的 | 重叠 ÷ 较矮盒高 |
|---|---|
| 页眉 正确率 [0.100, 0.113] 与 文体 [0.110, 0.122]（**应同行**） | **26%** |
| 正文相邻两行 [0.148, 0.176] 与 [0.169, 0.193]（**不应同行**） | **29%** |

26% 与 29%——**应同行的重叠得比不该同行的还少**。原因是倾斜把正文行的
轴对齐盒撑到 0.024–0.028（真行高约 0.012），撑出来的那一项把相邻行也叠上了。

## 为什么「按 top 聚类」也不行

我算过贪心聚类需要多大的阈值（按「与本簇起点的距离」聚）：

- 页眉标签行 tops 0.100 / 0.103 / 0.106 / 0.109 / 0.110 / 0.110，簇内铺开 **0.010**；
- 值行 0.122 / 0.125 / 0.128 / 0.129 / 0.131 / 0.131，簇内铺开 **0.009**；
- 两行之间（0.100 → 0.122）**0.022**；
- 正文行距（0.148 → 0.169）**0.021**。

要和正文分开，阈值必须 < 0.021；要把标签行收住，阈值必须 ≥ 0.010。
窗口 [0.010, 0.021) 看着够宽，**但同一页的选项区把它废掉了**：
`B` (0.704) 与 `C` (0.707) 只差 **0.003**，任何 ≥ 0.010 的阈值都会把这两个
**不同列不同行**的选项并成一簇，按 `minX` 排就是 `A, C, B, D` ——比现在还错。

**同一页上，表头需要 ~0.016 的阈值，选项区需要 < 0.003 的阈值。** 一个全局的
top 阈值不可能同时满足。这正是「局部弯曲」的另一种说法。

## 试过并否掉的（不要再试）

- **按 `center` 排序代替 `anchorTop`**：`center` 对**倾斜**是不变量（轴对齐盒的
  中点等于真中点），但对**弯曲**不是——弯曲让同一行在不同 x 处的真高度不同，
  中点照样跟着跑。实测表头仍然倒序，选项区那一对仍然分不开。
- **平局时按 `minX` 打破**：`D` (0.503) 会排到第 3 题题干 (0.111) 之后，
  和现在的错误一致。至少能让结果**确定**（现在是不确定的），但不修正它。
- **`fragmentOverlapFraction`（0.4）**：按碎片重叠判行，数值见上表，26% vs 29%，过不去。
- **用表格边框**：这页的表格确实有边框（裁图可见），但我们没在做线检测；
  `VNDetectRectanglesRequest` 或轮廓检测是另一条路，尚未评估。

## 解锁条件

**用户决定开始做页面矫正。** 届时的第一步**不是**写排序，而是**把弯曲量出来**：
按 y 分层（例如每 0.1 页高一层）拟合 `top` 对 `x` 的斜率，看它是不是一条光滑曲线、
需要几阶模型（线性 / 二次）。**在那之前不要挑模型，也不要写阈值**——
这个文件里已经记着两次「在合成图上标定、真机行为不符、返工」的教训。

拿到模型之后再决定去斜放在哪一层：`visualLines` 的排序键、还是更上游
（`TextRecognitionService` 出口，让所有下游都拿到已去斜的 `top`）。后者更彻底，
但要评估对 `ordercheck`（它钉的是 Vision 坐标里那组不变量）的影响。

## 需要的正样本

一张**同一视觉行横跨页面被弯到不同高度**的照片——`IMG_0004` 本身就是，
但只有这一张，量不出「弯曲随 y 怎么变」。再多一两张不同俯仰角的，
才够判断模型阶数。

## 怎么复现上面这几组数

`/tmp/pf-blocks/`（**一次性产物，未入库**）转储
`blocks(in: pageBody(in: lines))` 的每个块、每个视觉行，带 `top` / `height` /
`bottom` / x 区间。编译清单照 `tools/ocr-bench/build.sh` 里 paracheck 那一节
（`TextRecognitionService.swift` + `OCRImageSource.swift` + `RecognitionLanguage.swift`
+ `TextLayout.swift`，模拟器目标，经 `xcrun simctl spawn <UDID>` 跑）。

**喂图之前记得套 EXIF 朝向**，否则拿到的「行」其实是竖排的列。
